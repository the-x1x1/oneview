{
  "name": "@worldview/connector-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "The connectors that ship with WORLDVIEW (REST JSON, WebSocket JSON, GeoJSON, CSV) and the registry that turns a definition into a provider.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@worldview/connector-sdk": "workspace:*",
    "@worldview/provider-sdk": "workspace:*",
    "@worldview/world-model": "workspace:*"
  },
  "sideEffects": false
}
