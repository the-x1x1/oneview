{
  "name": "@worldview/render-dense",
  "version": "0.1.0",
  "private": true,
  "description": "Dense-layer abstraction (points/lines/heatmap), feature budgets and the presentation benchmark harness. Native adapters first; deck.gl only if benchmarks justify it (ADR-008).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@worldview/render-core": "workspace:*",
    "@worldview/world-model": "workspace:*"
  },
  "sideEffects": false
}
