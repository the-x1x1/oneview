{
  "name": "@worldview/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "WorldRuntime composition: providers + state + history + events + search + offline + cameras behind the IPC request catalogue.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc -p tsconfig.json" },
  "dependencies": { "@worldview/world-model": "workspace:*", "@worldview/ipc-contract": "workspace:*" },
  "sideEffects": false
}
