{
  "name": "@worldview/provider-firms",
  "version": "0.1.0",
  "private": true,
  "description": "NASA FIRMS active fire detections (VIIRS / MODIS near-real-time area CSV). Requires a per-user MAP_KEY; acknowledgement required.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc -p tsconfig.json" },
  "dependencies": { "@worldview/world-model": "workspace:*", "@worldview/provider-sdk": "workspace:*" },
  "sideEffects": false
}
