# Release fixtures

`sample-pnpm-lock.yaml` is a hand-authored pnpm lockfile (v9 shape) used by the
release tooling tests. Integrity hashes are obviously synthetic placeholders — the
tests assert parsing and SBOM shape, never real package contents.
